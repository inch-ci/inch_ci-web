<!DOCTYPE html>
<html lang="en">
  <%= render "layouts/head" %>

  <body class="repo">

    <div class="site-wrapper">

      <div class="site-wrapper-inner">

        <div class="cover-container">

          <div class="masthead clearfix">
            <div class="inner">
              <h3 class="masthead-brand"><a href="/"># Inch CI</a></h3>
              <ul class="nav masthead-nav">
                <% if logged_in? %>
                  <li class="nav <%= current_page?(user_path(current_user)) ? 'active' : nil %>">
                    <%= link_to current_user.name, user_path(current_user) %>
                  </li>
                <% end %>
                <% if @project %>
                  <li class="nav <%= @project && current_page?(project_path(@project)) ? 'active' : nil %>">
                    <%= link_to t("navigation.report_card"), params %>
                  </li>
                <% end %>
                <li class="nav">
                  <%= link_to t("navigation.what_is_this"), about_path %>
                </li>
                <!--
                <% unless logged_in? %>
                  <li>
                    <%= link_to icon(:github)+' '+t("navigation.signin"), signin_path, :class => 'btn btn-signin' %>
                  </li>
                <% end %>
                -->
              </ul>
            </div>
          </div>

          <div class="inner cover">
            <%= yield %>
          </div>
        </div>
      </div>
      <div class="site-wrapper-inner footer-container">
        <div class="cover-container">
          <%= render "layouts/footer" %>
        </div>
      </div>

    </div>
    <%= render "shared/ga" %>
  </body>
</html>
